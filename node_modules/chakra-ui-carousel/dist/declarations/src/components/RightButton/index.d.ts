import { ButtonProps, ComponentWithAs, IconProps } from "@chakra-ui/react";
import React from "react";
interface CustomButtonProps extends ButtonProps {
    customIcon?: ComponentWithAs<"svg", IconProps> | React.Element;
}
export declare const RightButton: ({ customIcon, ...props }: CustomButtonProps) => React.JSX.Element;
export {};
